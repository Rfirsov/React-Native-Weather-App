import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  weatherIconView: {
    display: "flex",
    alignItems: "center",
    marginVertical: 30,
  },
});

export default styles;
